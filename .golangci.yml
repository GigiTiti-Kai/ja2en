version: "2"

run:
  timeout: 3m

linters:
  default: none
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
    - gosec
    - misspell
    - revive

formatters:
  enable:
    - gofmt
    - goimports

issues:
  max-same-issues: 0
